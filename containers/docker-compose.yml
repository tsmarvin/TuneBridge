services:
  bridgebeats:
    image: tsmarvin/bridgebeats:develop
    container_name: bridgebeats
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - internal
    environment:
      - DOMAIN=${DOMAIN:-bridgebeats.link}
      - NODE_NUMBER=${NODE_NUMBER:-0}
      - DEFAULT_LOGLEVEL=${DEFAULT_LOGLEVEL:-Information}
      - HOSTING_DEFAULT_LOGLEVEL=${HOSTING_DEFAULT_LOGLEVEL:-Information}
      - APPLE_TEAM_ID=${APPLE_TEAM_ID:-}
      - APPLE_KEY_ID=${APPLE_KEY_ID:-}
      - APPLE_KEY_PATH=${APPLE_KEY_PATH:-}
      - SPOTIFY_CLIENT_ID=${SPOTIFY_CLIENT_ID:-}
      - TIDAL_CLIENT_ID=${TIDAL_CLIENT_ID:-}
      - ATPROTO_IDENTIFIER=${ATPROTO_IDENTIFIER:-}
      - ATPROTO_USER_DID=${ATPROTO_USER_DID:-}
      - ATPROTO_PDS_URI=${ATPROTO_PDS_URI:-https://pds.bridgebeats.link}
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - CACHE_DAYS=${CACHE_DAYS:-7}
      - CARD_CACHE_EXPIRATION_HOURS=${CARD_CACHE_EXPIRATION_HOURS:-1}
      - CARD_CACHE_CLEANUP_INTERVAL=${CARD_CACHE_CLEANUP_INTERVAL:-500}
      - REFRESH_INTERVAL_HOURS=${REFRESH_INTERVAL_HOURS:-24}
      - MAX_RECORDS_PER_RUN=${MAX_RECORDS_PER_RUN:-100}
      - IDENTITY_CONNECTION_STRING=${IDENTITY_CONNECTION_STRING:-Data Source=/app/data/bridgebeats.db}
      - RATE_LIMIT_REQUESTS_PER_HOUR=${RATE_LIMIT_REQUESTS_PER_HOUR:-20}
    volumes:
      - ./data/app:/app/data # Persistent data directory (sqlite + Aspire store)
      - ./logs:/app/data/logs # Logs bind-mounted to host for easy access
      - ./data/dp-keys:/app/keys # Data Protection key persistence
      # Secret mounts (read-only)
      - ./secrets/apple_key.p8:/run/secrets/apple_key:ro
      - ./secrets/spotify_client_secret.txt:/run/secrets/spotify_client_secret:ro
      - ./secrets/tidal_client_secret.txt:/run/secrets/tidal_client_secret:ro
      - ./secrets/discord_token.txt:/run/secrets/discord_token:ro
      - ./secrets/atproto_password.txt:/run/secrets/atproto_password:ro
      - ./secrets/atproto_oauth_key.json:/run/secrets/atproto_oauth_key:ro
      - ./secrets/api_key_salt.txt:/run/secrets/api_key_salt:ro
      - ./secrets/redis_password.txt:/run/secrets/redis_password:ro
      - ./secrets/internal_service_key.txt:/run/secrets/internal_service_key:ro
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://localhost:10000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G
        reservations:
          cpus: '0.5'
          memory: 256M

  redis:
    image: redis:8-alpine
    container_name: bridgebeats-redis
    restart: unless-stopped
    networks:
      - internal
    command:
      - sh
      - -c
      - |
        if [ -f /run/secrets/redis_password ]; then
          password=$$(head -n 1 /run/secrets/redis_password | tr -d '\015\012')
          printf 'requirepass %s\nappendonly yes\n' "$$password" > /tmp/redis.conf
          exec redis-server /tmp/redis.conf
        else
          exec redis-server --appendonly yes
        fi
    volumes:
      - ./data/redis:/data
      - ./secrets/redis_password.txt:/run/secrets/redis_password:ro
    healthcheck:
      test:
        - CMD-SHELL
        - |
          if [ -f /run/secrets/redis_password ]; then
            password=$$(head -n 1 /run/secrets/redis_password | tr -d '\015\012')
            REDISCLI_AUTH="$$password" redis-cli --no-auth-warning ping | grep -q PONG
          else
            redis-cli ping | grep -q PONG
          fi
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 6G
        reservations:
          cpus: '0.1'
          memory: 64M

  bridgebeats-pds:
    image: ghcr.io/bluesky-social/pds:latest
    container_name: bridgebeats-pds
    restart: unless-stopped
    networks:
      - internal
    volumes:
      - ./data/pds:/pds
    environment:
      - PDS_HOSTNAME=${PDS_HOSTNAME:-}
      - PDS_JWT_SECRET=${PDS_JWT_SECRET:-}
      - PDS_PLC_ROTATION_KEY_K256_PRIVATE_KEY_HEX=${PDS_PLC_ROTATION_KEY_K256_PRIVATE_KEY_HEX:-}
      - PDS_ADMIN_PASSWORD=${PDS_ADMIN_PASSWORD:-}
      - PDS_DATA_DIRECTORY=/pds
      - PDS_BLOBSTORE_DISK_LOCATION=/pds/blocks
      - PDS_EMAIL_SMTP_URL=${PDS_EMAIL_SMTP_URL:-smtp://username:clientsecret@smtp.azurecomm.net:587/}
      - PDS_EMAIL_FROM_ADDRESS=${PDS_EMAIL_FROM_ADDRESS:-}
      - PDS_BLOB_UPLOAD_LIMIT=52428800
      - PDS_DID_PLC_URL=https://plc.directory
      - PDS_BSKY_APP_VIEW_URL=https://api.bsky.app
      - PDS_BSKY_APP_VIEW_DID=did:web:api.bsky.app
      - PDS_REPORT_SERVICE_URL=https://mod.bsky.app
      - PDS_REPORT_SERVICE_DID=${PDS_REPORT_SERVICE_DID:-}
      - PDS_CRAWLERS=https://bsky.network
      - LOG_ENABLED=true

  bridgebeats-caddy:
    image: tsmarvin/caddy-cloudflare:develop
    container_name: bridgebeats-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    networks:
      - public
      - internal
    environment:
      - DOMAIN=${DOMAIN:-bridgebeats.link}
      - SHORT_DOMAIN=${SHORT_DOMAIN:-bbeats.link}
      - PDS_HOSTNAME=${PDS_HOSTNAME:-pds.bridgebeats.link}
      - CADDY_ADMIN_EMAIL=${CADDY_ADMIN_EMAIL:-admin@bridgebeats.link}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - bridgebeats-caddy-data:/data/caddy
      - bridgebeats-caddy-config:/config/caddy
      - ./logs/caddy:/var/log/caddy

volumes:
  bridgebeats-caddy-data:
    driver: local
  bridgebeats-caddy-config:
    driver: local

networks:
  public:
    driver: bridge

  internal:
    driver: bridge
