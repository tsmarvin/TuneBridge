namespace BridgeBeats.Core.Infrastructure.Extensions {

    // AddBridgeBeatsInfrastructure was removed: it had zero call sites and its registrations
    // are handled directly in each host's composition root. The file is retained as a namespace
    // placeholder for future extension methods in this layer.
}
